# Quick Start

## 🚀 最常用命令

```bash
# 启动开发服务器（热更新）
make dev

# 重启开发服务器
make restart

# 停止所有服务器
make kill
```

## ⚡ 开发流程

```
1. make dev          # 启动开发环境
2. 编辑代码           # 在 packages/*/src 中编辑
3. 浏览器自动刷新     # Vite HMR
4. make restart      # 如果热更新失败
5. make verify       # 提交前验证
```

## 📋 完整命令列表

运行 `make` 或 `make help` 查看所有命令。

## 🐛 遇到问题？

### 热更新不工作
```bash
make restart
```

### 构建错误
```bash
make typecheck   # 查看类型错误
make build       # 重新构建
```

### 完全重置
```bash
make reset       # 清理并重新安装一切
```

## 📖 详细文档

- [DEVELOPMENT.md](./DEVELOPMENT.md) - 完整开发指南
- [README.md](./README.md) - 项目说明
